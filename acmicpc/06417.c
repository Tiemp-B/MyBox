#include <stdio.h>

#define upbound 1000001

int main(){
    int N, K;
    int arr[upbound]={};
    for(int i=1; ){

    }

    while(scanf("%d", &N) && N!=-1){


        printf("%d coconuts, %d people and 1 monkey\n", N, 3);
    }
    return 0;
}