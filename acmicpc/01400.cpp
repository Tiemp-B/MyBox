#include <iostream>
#include <vector>
#include <queue>

using namespace std;



int main(){
    int N, M;
    
    while(true){
        cin >> N >>M;
        if(N==0) break;
        vector<vector<char>> field(N, vector<char>(M, '\0'));
        vector<vector<bool>> been(N, vector<bool>(M, false));
    }
    




    return 0;
}